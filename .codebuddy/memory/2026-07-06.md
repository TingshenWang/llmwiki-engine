# 2026-07-06

## lite-reasoning CLI Panel 优化
- 将 reasoning 显示从 `▌ 深度思考` 文本标记改为 Rich Panel 包裹（带边框、标题）
- 单调用步骤：用 `Rich Live` + `Panel` 实现流式实时更新（首个 chunk 启动 Live，后续 chunk 更新 Panel，结束 stop）
- 并行步骤：直接 `console.print(Panel(...))` 打印完整思维链块，Lock 保护
- Panel 配置：`border_style="cyan"`, `title_align="left"`, `padding=(0, 1)`, 文本 `style="dim"`
- 全部 91 个测试通过

## deepseek-v4-flash 确认支持 reasoning
- DeepSeek V4 Flash 和 V4 Pro 都支持 thinking 模式，默认开启，返回 `reasoning_content` 字段
- 旧的 `deepseek-reasoner` 将在 2026-07-24 退役，`deepseek-v4-flash` 是替代
- thinking 模式下 `temperature` 不生效（设了也不报错）
- 不应擅自将 vault config 从 `deepseek-v4-flash` 改为 `deepseek-reasoner`——全局配置的 `deepseek-v4-flash` 本身就返回 reasoning_content

## 3 篇 E2E 测试（deepseek-v4-flash + --reasoning）
- 测试 vault：`~/Desktop/🧠reasoning-panel-demo`（用全局 deepseek-v4-flash 配置）
- 3 篇全部成功：43 个 reasoning.md、8 个 wiki 页面、覆盖率 100%
- 缓存命中率 27-43%（比 deepseek-reasoner 的 6-20% 高）
- 从 reasoning 内容分析出 5 个 prompt 设计问题（见飞书需求清单 LWM-047~051）

## 飞书需求清单更新
- 文档：`https://sensetime.feishu.cn/wiki/Shhsw4RkeiR55wkKGvRcd0ppnNf`（多维表格）
- bitable app_token：`MS6abvYNdas5iKsKW44cfieVnxf`（需用户提供，API 无法自动解析）
- table_id：`tblsYjHKVzVPpTZH`，view_id：`vew2IwHmj4`
- 新增 6 条记录（LWM-047~052）：5 个 prompt 问题 + 1 个 reasoning 功能记录
- 用 `lark-cli api POST/PUT` 原生 API 操作（比 base shortcut 更可靠）

## feishu-codebuddy skill 创建
- 位置：`~/.codebuddy/skills/feishu-codebuddy/`
- 结构：SKILL.md（入口+路由+通用坑点）+ references/（5 个功能模块独立文件）
- 模块：bitable.md（最详细，含 token 解析坑点）、docs.md、comments.md、calendar.md、meeting.md
- 遵循渐进式披露：SKILL.md 只放入口信息，详细步骤按需加载 reference 文件
- lark-cli 应用配置：appId=cli_aaa50792b9fa1ce6（用户的应用，非 WorkBuddy）
- 已授权 scope：wiki:node:retrieve, base:record:read, base:table:read, base:app:read, bitable:app:readonly, bitable:app
