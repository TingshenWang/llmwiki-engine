# 2026-07-03

## GitHub 仓库连接
- 将 GitHub 仓库 `git@github.com:TingshenWang/llmwiki-engine.git` 克隆到工作区 `/Users/wangtingshen/tingshen-local-llmwiki`
- 过程中遇到 SSH host key verification 失败，通过 `ssh-keyscan` 将 github.com 主机密钥添加到 `~/.ssh/known_hosts` 解决
- SSH 认证用户：`TingshenWang`，密钥：`~/.ssh/id_ed25519`
- 项目 `llmwiki-engine`，当前分支 `Lite`（跟踪 origin/Lite），版本 v0.2.4
- 远程分支：Lite（默认）、dev、codex/ingest-quality-speed
- 项目使用 `uv` 管理依赖（pyproject.toml + uv.lock），包含 src/、scripts/、tests/ 目录
- 运行测试需 `uv run --extra dev python -m pytest`（pytest 在 optional-dependencies dev 中）

## lite-reasoning 分支开发
- 从 `Lite` 分支切出 `lite-reasoning` 分支，为 DeepSeek reasoner 模型增加思维链保存与 CLI 流式实时显示
- 修改三个源文件 + 新增一个测试文件：
  - `src/llmwiki_engine/lite/providers.py`：ProviderSpec 加 `stream_reasoning` 字段；ProviderCallResult 加 `reasoning_content`；`call_structured` 加 `reasoning_callback` 参数；`_call_openai_compatible` 新增流式路径（`stream_once` 闭包，解析 SSE delta.reasoning_content + delta.content，累积后重建 raw_response）；`build_chat_payload` 加 `stream` 参数；`_compact_response` 保留 `reasoning_content`；新增 `_extract_reasoning_content` 和 `_is_json_schema_unavailable_text` 辅助函数
  - `src/llmwiki_engine/lite/pipeline.py`：`run_ingest` 加 `show_reasoning` 参数；state dict 加 `show_reasoning` 和 `console`；`_run_step` 加 `show_reasoning`（为 True 时禁用 spinner）；`_call_provider_artifact_soft` 构建 reasoning_callback 并保存 `.reasoning.md` artifact；两个并行函数（`_call_provider_artifacts_parallel` / `_parallel_soft`）保存 `.reasoning.md` 并用 `threading.Lock` 保护按块打印 reasoning
  - `src/llmwiki_engine/cli.py`：`ingest_run` 加 `--reasoning` flag，`--json` 模式覆盖（show_reasoning = reasoning and not json_output）
  - `tests/test_lite_reasoning.py`：16 个测试（providers 7 + pipeline 6 + cli 3），全部通过
- 设计要点：第一层始终从非流式响应提取 reasoning_content 并保存 artifact（零风险）；第二层 `--reasoning` 开关启用流式实时显示；并行步骤用非流式+完成时按块显示（Lock 保护避免交错）

## lite-reasoning 端到端测试
- 测试 vault：`~/Desktop/lite-reasoning-test-vault`，配置 deepseek-reasoner 模型
- 3 篇文章来自 `~/Desktop/Lite-E2E-Seed-3Docs-20260617-233440/raw/`（Cat Wu 访谈、Scaling Managed Agents、Stop Applying to AI PM Jobs）
- 全部 3 篇 ingest 成功完成（`--reasoning` 模式），共生成 55 个 `.reasoning.md` artifact + 55 个 provider_result.json 含 reasoning_content + 14 个 wiki 页面
- 单调用步骤（source_digest、merge_plan、composition_plan 等）流式实时显示思维链正常；并行步骤（candidate_pages、final_pages、final_coverage_judge）按块显示正常
- 全局 `~/.llmwiki/config.yaml` 配置的是 deepseek-v4-flash，vault 级 config.yaml 可覆盖为 deepseek-reasoner
- embeddings 步骤需要 `sentence-transformers` 包（optional-dependencies），用 `uv pip install sentence-transformers` 安装

## TODO（待办，不急）
- CLI reasoning 显示需要加"深度思考开始/结束"的分隔提示符，当前流式打印思维链中间毫无分段，可读性不够好。修改位置在 `pipeline.py` 的 `_call_provider_artifact_soft` 中 `reasoning_callback` 闭包（当前只在首次打印 `▌ 思维链` 头标，结束时打印换行），需要改为有明确的开始和结束标记。
